{
  "name": "campusbite-functions",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth,storage,hosting",
    "deploy": "npm run build && firebase deploy --only functions",
    "watch": "tsc -w"
  },
  "main": "lib/index.js",
  "private": true,
  "dependencies": {
    "@cashfreepayments/cashfree-js": "^1.0.5",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-google": "^0.14.0",
    "typescript": "^5.4.0"
  }
}
